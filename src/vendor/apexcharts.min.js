/* Local ApexCharts placeholder. In real project place the actual minified ApexCharts here. For now we inject a stub to avoid network dependency. */
(function(global){
  if (global.ApexCharts) return; // if already provided by CDN
  function Stub(el, opts){
    this.el = el; this.opts = opts || {}; this._rendered = false;
  }
  Stub.prototype.render = function(){
    this._rendered = true;
    if (this.el) {
  this.el.innerHTML = '<div style="font:12px sans-serif;padding:8px;color:#ccc;border:1px dashed #555;text-align:center;">ApexCharts (stub) – chart not available offline</div>';
    }
    return Promise.resolve();
  };
  Stub.prototype.updateSeries = function(){ return Promise.resolve(); };
  global.ApexCharts = Stub;
})(window);